<%= form_for(@comentario_autenticado) do |f| %>
  <% if @comentario_autenticado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comentario_autenticado.errors.count, "error") %> prohibited this comentario_autenticado from being saved:</h2>

      <ul>
      <% @comentario_autenticado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comentario_id %><br>
    <%= f.text_field :comentario_id %>
  </div>
  <div class="field">
    <%= f.label :usuario_id %><br>
    <%= f.text_field :usuario_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
